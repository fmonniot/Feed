*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
/.idea/planningMode.xml
.DS_Store
/build
/shared/build
/app/build
/web/build
/captures
.externalNativeBuild
.cxx
local.properties
.kotlin

server/debug
server/target
server/*.db
server/*.db-shm
server/*.db-wal
server/config.toml

# Android release signing (ticket #47) — never commit the production keystore
# or its credentials. app/keystore.properties.example is the committed template.
*.keystore
*.jks
keystore.properties

# Temp directory used as a volume when testing the docker image
data/

# Screenshot tooling (ticket #75) — node deps. Output lives in /build/.shots (already ignored).
/scripts/shots/node_modules

# Keep .claude/plans/ tracked so multi-session work is shared; ignore everything else.
.claude/*
!.claude/plans/
!.claude/skills
